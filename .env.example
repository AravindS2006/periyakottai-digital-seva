# =============================================================================
# PERIYAKOTTAI DIGITAL SEVA - ENVIRONMENT CONFIGURATION TEMPLATE
# =============================================================================
# Copy this file to .env.local for local overrides.
# In Vercel, set these in Project Settings -> Environment Variables.

# Canonical Production URL (used by metadataBase, sitemap, OpenGraph, and PWA)
NEXT_PUBLIC_SITE_URL=https://periyakottai.vercel.app

# Node Environment
NODE_ENV=production

# Optional: Operator Custom Secret (defaults to standard authorized credentials)
# OPERATOR_PIN=624614
